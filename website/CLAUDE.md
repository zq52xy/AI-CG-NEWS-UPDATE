# Website/
> L2 | 父级: ../CLAUDE.md

## 成员清单

- `app.js`: 核心逻辑，负责加载 Markdown、渲染页面、处理历史记录和交互
- `index.html`: 入口文件，定义页面结构和基本元数据
- `style.css`: 样式表，包含响应式布局、深色模式配色和 Markdown 内容样式

## 子目录

- `src/hooks/`: React Hooks
- `src/network/`: HTTP 网络层（基于 ky）

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
